{
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "poc:jenkins-parallel": "node scripts/jenkins-parallel-poc.js"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.11.1",
    "vitest": "^3.2.4"
  }
}